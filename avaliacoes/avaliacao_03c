#include <stdio.h>
#include <stdlib.h>
 struct Dados {

char nome[50];
int idade;
float altura;
 };

int main () {
  struct Dados pessoa; 

  printf ("Digite seu nome");

  fgets (pessoa.nome, fgets
    50, stdin);
  //terminar chatgpt//
}
