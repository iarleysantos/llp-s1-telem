#include <stdio.h>

// Definição da estrutura
struct Dados {
  
   
int idade;
    
   
char nome[50];
};

int main() {
    
   
struct Dados dados;

    

// Receber os dados da estrutura
    
   
printf("Digite a idade: ");
    
   
scanf("%d", &dados.idade);

    

printf("Digite o nome: ");
    
   
scanf("%s", dados.nome);

    

// Abrir o arquivo para escrita
    FILE *arquivo = fopen(
    FILE *arquivo = fopen

    FILE *arquivo =

    FILE *arquivo

    FILE *ar

    FILE *

    FILE

   
"dados.txt", "w");
    
   
if (arquivo == NULL) {
        
       
printf("Erro ao abrir o arquivo.\n");
        
       
return 1;
    }

    
    }

   

    }

//Gravação da estrutura no arquivo

fprint(arquivo, "Idade: %d\n", dados.idade);
fprint(arquivo, "Nome: %s"\n, dados.nome);
fprint(arquivo, "Altura: %.2f\n", dados.altura);

//Fechamento do arquivo

fclose(arquivo);
fclose(arquivo);
fclose(arquivo);
fclose(ar);

printf("Estrutura gravada no arquivo com sucesso. \n");

return 0;

//falta terminar

 
