#include <stdio.h>
//definição de uma estrutura

struct Dados {
int idade;
char nome[50]; 
};
int main() {
  struc Dados dados;

  //receber os dados da estrutura
  
  printf("Digite seu nome:");
 
  scanf("%d", dados.nome);
  
  printf("Digite sua idade:");

  scanf("%d", &dados.idade);


  //abrir arquivo para a escrita

  FILE *arquivo = fopen(
  FILE *arquivo = fopen
  FILE *arquivo = 
  FILE *arquivo 
  FILE *ar
  FILE *
  FILE

  "dados.txt", "w");
  
  
}
 
